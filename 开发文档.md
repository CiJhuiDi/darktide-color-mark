# ColorMark（彩色标记）开发文档

> 状态：设计定稿，待开发 | 2026-08-12
> 关联：基于 AnonPlayers 的隐藏机制，为其扩展"标记特殊玩家显示指定颜色"功能

---

## 一、项目概述

### 功能目标

在 AnonPlayers 隐藏玩家名字的基础上，玩家通过**聊天命令**标记特殊玩家（固定队友/熟人），被标记玩家的名字在游戏内所有显示位置以**指定颜色**呈现，方便一眼识别。

### 核心原则

1. **不破坏 AnonPlayers 的隐藏**：未标记玩家完全走 AnonPlayers 逻辑；被标记玩家的显示方式可配置（默认保持匿名+颜色）
2. **标识稳定**：内部一律用**账号唯一 ID（account_id）**，绝不用名字匹配（生僻字/特殊符号不可输入）
3. **纯聊天交互**：不用 UI 开发（第一版），命令+编号选择玩家

### 名词

|     词      |                         含义                            |
|-------------|---------------------------------------------------------|
|   标记      | 把一个玩家加入彩色名单，指定颜色                           |
| account_id | 玩家账号唯一 ID（`RemotePlayer:account_id()`），标记的 key |
| 显示名      | 玩家名字（受 AnonPlayers 影响，可能被匿名化）              |

---

## 二、命令设计（DMF `mod:command`）

| 命令 | 参数 | 行为 |
|---|---|---|
| `/players` | 无 | 聊天栏列出当前房间玩家：`1. 显示名A  2. 显示名B ...` |
| `/mark <编号> <颜色>` | 编号=players 列表序号；颜色=预设名或 hex | 标记该玩家为指定颜色；重复标记=改颜色 |
| `/unmark <编号>` | 编号 | 取消标记 |
| `/marks` | 无 | 列出所有已标记：`[红] 显示名A  [蓝] 显示名B` |

**颜色解析**（优先级）：
1. 预设色板：`red 橙 orange 黄 yellow 绿 green 青 cyan 蓝 blue 紫 purple 粉 pink 白 white 灰 gray`
2. hex 格式：`/mark 2 ff8800`（RGB 十六进制，不带 #）

**边界**：
- 编号越界/非法 → 聊天提示"无效编号"
- 颜色无法解析 → 提示"无效颜色"
- 指向 bot（无账号）→ 提示"无法标记机器人"
- 列表为空（无其他玩家）→ 提示"当前房间没有其他玩家"

---

## 三、技术方案

### 3.1 数据流

```
/players 命令
  └→ Managers.player:players()（按 peer 遍历）
      └→ 每玩家：编号 + 显示名 player:name()（尊重 AnonPlayers 匿名）+ account_id（RemotePlayer:account_id()）
      └→ 聊天栏输出列表

/mark 命令
  └→ 解析编号 → 玩家对象 → account_id → 存标记表

名字渲染（每帧/每次显示）
  └→ hook 的名字函数被调用 → 拿到玩家对象/账号标识
      └→ 查标记表：命中 → 返回 {#color(r,g,b)}显示名{#reset()}
      └→ 未命中 → 调 func() 放行（走 AnonPlayers 匿名）
```

### 3.2 标记表（持久化）

```lua
-- mod:persistent_table("marks") 跨会话保存
{
  [account_id] = {
    color = { r, g, b },        -- 0-255
    name  = "显示名缓存",        -- 展示用（/marks 列表）
    time  = os.time(),          -- 标记时间
  },
}
```

- key = account_id（账号级：同账号多角色共用标记）
- bot 无 account_id，不可标记
- 自己（HumanPlayer）默认不可标记（可后续加选项）

### 3.3 名字 hook 清单（照抄 AnonPlayers，9 处）

| # | 对象 | 函数 | 拿 account_id 的方式 | 说明 |
|---|---|---|---|---|
| 1 | `PlayerInfo` | `character_name` | 查 PlayerInfo 的账号字段（待验证，备选 presence） | 社交/聊天/结算 |
| 2 | `PlayerInfo` | `user_display_name` | 同上 | ⚠️ AnonPlayers 在此用 hook_origin，**我们必须用普通 hook**（链尾是 origin） |
| 3 | `RemotePlayer` | `name` | `self:account_id()` ✅ | 头顶名字（hub+任务） |
| 4 | `RemotePlayer` | `character_name` | `self:account_id()` ✅ | 结算/名字显示 |
| 5 | `HumanPlayer` | `name` | 自己，跳过 | 自己 HUD 名字 |
| 6 | `BotPlayer` | `name` | 无账号，跳过 | bot |
| 7 | `PresenceEntryMyself` | `character_name` | 自己，跳过 | 邀请/组队 |
| 8 | `PresenceEntryImmaterium` | `character_name` | presence 的账号字段（待验证） | 邀请/组队 |
| 9 | `profile_utils` | `character_name` | ⚠️ 只有 profile！用 **character_id→account_id 映射表** | 预任务大厅/角色选择 |

**映射表**：每次 `/players` 或刷新时构建 `{ [character_id] = account_id }`（从玩家列表收集），供 #9 使用。

### 3.4 彩色名字

```
{#color(255,0,0)}红名玩家{#reset()}
```

- 聊天/结算富文本：✅ 已验证支持（For The Emperor 同款）
- 头顶 nameplate：⚠️ **待实测**；Plan B = hook nameplate 按账号改 text_color（不动字符串）

### 3.5 显示模式（设置项）

| 设置 | 默认 | 说明 |
|---|---|---|
| `mark_display_mode` | `anon_color` | 被标记玩家显示：`anon_color` 匿名名+颜色（直播安全）/ `real_color` 真名+颜色 / `label_color` 自定义标签+颜色 |
| `players_list_show_real` | `false` | /players 列表是否显示真实账号名（默认显示匿名名，尊重 AnonPlayers） |
| `color_presets` | 内置 | （可选）自定义预设色板 |

**真名获取**（real_color 模式用）：`RemotePlayer` 原始名来源——`Steam.user_name(peer_id)` 或 player_info（AnonPlayers 源码 118-139 行有现成逻辑可参考）。⚠️ 注意：AnonPlayers 开启时直接读真名会"泄露"，仅在被标记玩家且用户选择 real_color 时使用。

---

## 四、与 AnonPlayers 的共存规则

- **加载顺序**：无硬性要求（分析见下），但建议 ColorMark 排在 AnonPlayers **后面**（保险）
- **hook 链**：两 mod 都 hook 同一批函数（普通 hook）。执行顺序 = 加载顺序倒序：
  - 标记玩家：ColorMark 的 hook 返回彩色名（**截断链**，AnonPlayers 不执行）✓
  - 未标记玩家：ColorMark 调 `func()` 放行 → AnonPlayers 匿名化 ✓
- **无论谁先加载都成立**：因为 ColorMark 只拦截标记玩家，未标记一律放行
- ⚠️ `user_display_name`：AnonPlayers 用 hook_origin（独占替换），ColorMark 只用普通 hook（链在 origin 之前）→ 共存 OK

---

## 五、风险与验证清单

| 风险 | 等级 | 对策/验证 |
|---|---|---|
| nameplate 富文本不支持 | ⚠️ 中 | 进游戏实测头顶名字；Plan B hook nameplate text_color |
| PlayerInfo / PresenceEntry 的 account_id 字段未知 | ⚠️ 中 | 开发时读源码确认；备选：presence 对象 / 角色映射 |
| profile_utils 拿不到玩家对象 | ⚠️ 中 | character_id→account_id 映射表（/players 时构建） |
| 真名模式绕过 AnonPlayers 隐藏 | 低 | 默认关，仅标记玩家+用户显式开启 |
| 中文名富文本拼接 | 低 | 无特殊处理，纯字符串拼接 |

**功能验证清单**：
- [ ] `/players` 列表编号与显示名正确（AnonPlayers 开着时显示匿名名）
- [ ] `/mark` 后聊天/结算名字变色；`/marks` 正确列出；`/unmark` 恢复
- [ ] 重名/多角色玩家标记不错人（account_id 验证）
- [ ] 重启游戏后标记仍在（persistent_table）
- [ ] 与 AnonPlayers 同开：未标记玩家仍匿名，标记玩家彩色
- [ ] 头顶名字变色（或 Plan B 生效）
- [ ] hex 颜色 + 预设颜色都生效

---

## 六、开发任务拆解（建议顺序）

1. **骨架**：color_mark.mod / data（设置项）/ localization（命令文案、提示）
2. **颜色解析**：预设色板表 + hex 解析（纯函数，先行可测）
3. **玩家列表**：`/players`（players() 遍历 + 编号 + account_id 收集 + 映射表构建）
4. **标记管理**：`/mark` `/unmark` `/marks` + persistent_table
5. **名字 hook**：9 处 hook + 富文本包装（先做 RemotePlayer.name + PlayerInfo.character_name 两处核心验证）
6. **映射表接入**：profile_utils hook
7. **nameplate 验证**：进游戏实测，必要时 Plan B
8. **共存验证 + 部署**：与 AnonPlayers 同开测试，部署游戏 mods 目录

## 七、待定/遗留

- [ ] mod 最终命名（暂定 `color_mark`，显示名"彩色标记"）
- [ ] label_color 模式的自定义标签输入方式（可能用 `/mark <编号> <颜色> <标签>`）
- [ ] 是否支持标记自己（默认不支持）
- [ ] 色板自定义设置（可选二期）
