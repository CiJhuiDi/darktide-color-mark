# ColorMark（彩色标记）开发文档

> 状态：**v1.0.1（2026-08-20 real_color 修复，待实测）** | 2026-08-20
> 关联：基于 AnonPlayers 的隐藏机制，为其扩展"标记特殊玩家显示指定颜色"功能

---

## 一、项目概述

### 功能目标

在 AnonPlayers 隐藏玩家名字的基础上，玩家通过**聊天命令**标记特殊玩家（固定队友/熟人），被标记玩家的名字在游戏内所有显示位置以**指定颜色**呈现，方便一眼识别。

### 核心原则

1. **不破坏 AnonPlayers 的隐藏**：未标记玩家完全走 AnonPlayers 逻辑；被标记玩家的显示方式可配置（默认保持匿名+颜色）
2. **标识稳定**：内部一律用**账号唯一 ID（account_id）**，绝不用名字匹配（生僻字/特殊符号不可输入）
3. **纯聊天交互**：不用 UI 开发，命令+编号选择玩家

### 名词

|     词      |                         含义                            |
|-------------|---------------------------------------------------------|
|   标记      | 把一个玩家加入彩色名单，指定颜色                           |
| account_id | 玩家账号唯一 ID（`PlayerInfo:account_id()`），标记的 key |
| 显示名      | 玩家名字（受 AnonPlayers 影响，可能被匿名化）              |

---

## 二、选人方案演进（重要）

| 方案 | 结论 | 原因 |
|---|---|---|
| `/mark <steam名> <颜色>` | ❌ 否 | 生僻字/特殊符号不可输入；暴露隐私 |
| 准星指向选人 | ❌ 否 | **活人会跑**，不可能一直拿准星指着目标 |
| 编号列表（/players 房间玩家） | ❌ 否 | AnonPlayers 开着时列表全是匿名名，认不出人 |
| **队伍列表（/squad）** | ✅ **定稿** | 标记对象=熟人=当前队伍成员；`fetch_party_members()` 返回 PlayerInfo **真名**（mod 内部拿数据，不受 AnonPlayers UI 隐藏影响），人少好选，编号稳定 |

曾经候选过的 friends（好友）/ recent（最近队友）列表：功能可用但**臃肿**，用户拍板砍掉，只保留队伍查询。

---

## 三、命令设计（DMF `mod:command`）

| 命令 | 参数 | 行为 |
|---|---|---|
| `/squad` | 无 | 列出当前队伍成员：`1. 真名A  2. 真名B ...`（排除自己） |
| `/mark <编号> <颜色>` | 编号=/squad 列表序号；颜色=预设名或 hex | 标记该玩家；重复标记=改颜色 |
| `/unmark <编号>` | 编号 | 取消标记 |
| `/marks` | 无 | 列出所有已标记：`[红] 真名A  [蓝] 真名B` |

**颜色解析**（优先级）：
1. 预设色板：`red 橙 orange 黄 yellow 绿 green 青 cyan 蓝 blue 紫 purple 粉 pink 白 white 灰 gray`
2. hex 格式：`/mark 2 ff8800`（RGB 十六进制，不带 #）

**边界**：
- 未先 `/squad` 或编号越界 → 提示"无效编号，先运行 /squad"
- 颜色无法解析 → 提示"无效颜色"
- 队伍为空/无其他玩家 → 提示"当前队伍没有其他玩家"
- 任务中（is_in_mission）`fetch_party_members` 自动用任务内玩家表（源码已处理）

---

## 四、技术方案

### 4.1 数据流

```
/squad 命令
  └→ Managers.data_service.social:fetch_party_members()  (Promise)
      └→ { unique_id → PlayerInfo }（去重、剔除 bot、剔除自己）
      └→ 每成员：编号 + 真名（PlayerInfo:character_name()，_account_name 兜底）
      └→ 缓存到 mod.squad_list + 输出聊天列表

/mark 命令
  └→ 编号 → mod.squad_list[编号].account_id → 存标记表

名字渲染（每帧/每次显示）
  └→ hook 的名字函数被调用 → 拿到玩家对象/账号标识
      └→ 查标记表：命中 → 返回 {#color(r,g,b)}显示名{#reset()}
      └→ 未命中 → 调 func() 放行（走 AnonPlayers 匿名）
```

### 4.2 标记表（持久化）

```lua
-- mod:persistent_table("marks") 跨会话保存
{
  [account_id] = {
    color = { r, g, b },        -- 0-255
    name  = "真名缓存",          -- 展示用（/marks 列表）
    time  = os.time(),          -- 标记时间
  },
}
```

- key = account_id（账号级：同账号多角色共用标记）
- bot 无 account_id，不可标记
- 自己（HumanPlayer）默认不可标记

### 4.3 名字 hook 清单（照抄 AnonPlayers）

| # | 对象 | 函数 | 拿 account_id 的方式 | 说明 |
|---|---|---|---|---|
| 1 | `RemotePlayer` | `name` | `self:account_id()` ✅ | 头顶名字（hub+任务） |
| 2 | `RemotePlayer` | `character_name` | `self:account_id()` ✅ | 结算/名字显示 |
| 3 | `PlayerInfo` | `character_name` | `self:account_id()` ✅（源码已确认） | 社交/聊天/结算 |
| 4 | `PlayerInfo` | `user_display_name` | 同上 | ⚠️ AnonPlayers 在此用 hook_origin，本 mod 必须用普通 hook |
| 5 | `PresenceEntryImmaterium` | `character_name` | `self:account_id()` ⚠️ 是 platform_user_id | 邀请/组队 + **哀星号左下角队伍面板**（PartyImmateriumMember:name → presence:character_name）；2026-08-22 补 real_color 真名逻辑（旧版只上色 func 结果 = 彩色掩码） |
| 6 | `profile_utils` | `character_name` | character_id→account_id 映射表 | 预任务大厅/角色选择；`/squad` 时构建映射 |

- `HumanPlayer.name` / `BotPlayer.name` / `PresenceEntryMyself.character_name`：自己/bot/自己，跳过不 hook

### 4.4 彩色名字

```
{#color(255,0,0)}红名玩家{#reset()}
```

- 聊天/结算富文本：✅ 已验证支持（For The Emperor 同款）
- 头顶 nameplate（hub）：✅ 源码确认支持（world_marker_template_nameplate/party_hud 用 `data:name()` = RemotePlayer:name()，渲染走 `Gui2_slug_text` slug 富文本）
- 任务（combat）头顶：❌ 仍掩盖（combat 模板用私有 name_and_title 逻辑不走 name()，2026-08-20 确认）

### 4.5 显示模式（设置项）

| 设置 | 默认 | 说明 |
|---|---|---|
| `mark_display_mode` | `anon_color` | `anon_color` 匿名名+颜色（直播安全）/ `real_color` 真名+颜色（仅标记玩家生效，绕过 AnonPlayers） |

真名来源（real_color）：标记时缓存的 `mark.name`（/squad 直读）→ `RemotePlayer._cached_name` → `Steam.user_name(peer_id)`；PlayerInfo 用 `mark.name` → `_account_name`。

#### ⚠️ 平台图标坑（2026-08-22）

AnonPlayers `show_other_platform=true` 时 `user_display_name()` 返回「平台图标(U+E06B 私有区字符)+空格+真名」，/mark 存真名时图标被一起存进 mark.name。显示前必须剥：富文本标记 `{#...}` + 私有区字符（U+E000-U+F8FF）+ 首尾空格 trim。否则名牌/队伍面板显示异常字符。migrate_marks 复用新 strip 自动清洗旧标记。

**⚠️ 私有区剥离必须按完整 3 字节序列匹配（2026-08-22 修复）**：U+E000-U+EFFF = `\xEE[\x80-\xBF][\x80-\xBF]`，U+F000-U+F8FF = `\xEF[\x80-\xA3][\x80-\xBF]`（EF A4 起是 CJK 兼容表意文字，不能剥）。**禁止写成单字节范围 `[\xEE\x80\x80-\xEE\xBF\xBF...]`**：Lua 字符集按字节解析，`\x80\x80-\xEE` 会被解析成字节范围 0x80-0xEE，最终集合覆盖 0x80-0xEF 全部字节 = **所有 UTF-8 多字节字符（中文/日文/韩文）全被剥光** → 中文名玩家 mark.name 被剥成空串 → real_color 不显示真名（已实测复现，验证脚本 `暗潮\99-临时文件\verify_strip_regex.py`）。

#### ⚠️ real_color 与直播安全（2026-08-20 用户确认的风险）

**结论：`real_color` 显示的就是账号名（Steam 昵称），直播时开启有炸直播风险。**

原因链：

1. AnonPlayers 存在的原因就是防直播被违规名炸号——Steam 昵称可以被随意改成违规词，观众看到就会炸直播。
2. AnonPlayers 是本地 mod，只隐藏**自己客户端**的显示，不影响队友看到的；但直播画面 = 自己客户端画面，`real_color` 把账号名显示出来，观众能看到。
3. **角色名（游戏内名字）在 AnonPlayers 匿名模式下被 hook 链挡住，根本拿不到**；能拿到的"真名"只有账号名（`_account_name` / `_cached_name` / `Steam.user_name`）。
4. 所以"解除匿名"与"匿名存在的理由"本质矛盾：绕过匿名 = 只能显示账号名，而账号名可能违规。

**使用建议（用户确认口径）：**

- 直播时用默认 `anon_color`：显示 `???`+颜色，靠颜色认人，完全安全。
- `real_color` 仅限非直播/单机使用，风险自担。
- 标记的熟人中 Steam 名本身干净时，`real_color` 也安全。

---

## 五、与 AnonPlayers 的共存规则

- **加载顺序**：无硬性要求，建议 ColorMark 排在 AnonPlayers **后面**（保险）
- **hook 链**：两 mod 都 hook 同一批函数（普通 hook）。执行顺序 = 加载顺序倒序：
  - 标记玩家：ColorMark 的 hook 返回彩色名（**截断链**，AnonPlayers 不执行）✓
  - 未标记玩家：ColorMark 调 `func()` 放行 → AnonPlayers 匿名化 ✓
- **无论谁先加载都成立**：因为 ColorMark 只拦截标记玩家，未标记一律放行
- ⚠️ `user_display_name`：AnonPlayers 用 hook_origin（独占替换），ColorMark 只用普通 hook（链在 origin 之前）→ 共存 OK

---

## 六、风险与验证清单

| 风险 | 等级 | 对策/验证 |
|---|---|---|
| nameplate 富文本 | ✅ 已确认 | hub 名牌走 RemotePlayer:name + Gui2_slug_text slug 渲染（2026-08-22 源码确认）；combat 模板不走 name()，任务头顶仍掩盖 |
| PresenceEntryImmaterium hook 缺真名逻辑 | ✅ 已修 | 2026-08-22：旧版只上色 func 结果 = 彩色掩码（AnonPlayers 已匿名）；已补 real_color 替换 + 日志 [cm][PEI:char_name] |
| 平台图标存进 mark.name | ✅ 已修 | 2026-08-22：strip_rich_text 剥私有区字符 U+E000-U+F8FF + trim；migrate 自动清洗旧标记 |
| PresenceEntryImmaterium 的 account_id 是平台 ID | ⚠️ 低 | Steam 下大概率相等；匹配不到放行不崩 |
| fetch_party_members 在 hub 外/单人的表现 | ⚠️ 低 | 源码已处理任务内；单人时列表为空提示 |
| 真名模式绕过 AnonPlayers 隐藏 | 低 | 默认关，仅标记玩家+用户显式开启 |

**功能验证清单**：
- [ ] `/squad` 列表编号与真名正确（hub 组队时；任务中显示任务内队友）
- [ ] `/mark 编号 颜色` 后聊天/结算名字变色；`/marks` 正确列出；`/unmark` 恢复
- [ ] 未 `/squad` 直接 `/mark` 提示无效编号
- [ ] 重名/多角色玩家标记不错人（account_id 验证）
- [ ] 重启游戏后标记仍在（persistent_table）
- [ ] 与 AnonPlayers 同开：未标记玩家仍匿名，标记玩家彩色
- [ ] 头顶名字变色（或 Plan B 生效）
- [ ] hex 颜色 + 预设颜色都生效

---

## 七、文件与仓库

- 本地：`D:\DeepseekWorkspace\暗潮\04-Mods\color_mark`
- 仓库：https://github.com/CiJhuiDi/darktide-color-mark（**已建仓未推送**，本地 commit 02c0490 → ff237ca）
- 发布包：待实测通过后打 zip（DMM 导入）

## 八、遗留

- [ ] 实测：哀星号左下角队伍面板显示彩色真名（2026-08-22 修复后待用户重启验证）
- [ ] 实测通过后：bump 版本（1.0.1 → 1.0.2）+ 打包 zip + 推送仓库 + Release
